[tool.ruff]
target-version = "py312"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "W", "I"]
ignore = ["E501"]

[tool.ruff.lint.per-file-ignores]
# SQLAlchemy forward references in strings are valid — ruff не может их разрешить
"app/models/*.py" = ["F821"]
